// Task 1
let line = "Stand so high Huge hooves too Impatiently waits for Reins and harness Eager to leave";
const frontDoorLetter = line[0]; 

// Task 2
const frontDoorPassword = frontDoorLetter + line[14].toLowerCase() + line[30].toLowerCase() + line[52].toLowerCase() + line[80].toLowerCase()
let frontDoorWord = frontDoorPassword

// Task 3
let lineWithWhiteSpace = 'Reins and harness'
let backDoorLetter = lineWithWhiteSpace[16]

// Task 4
let backDoorPassword = `Hor${backDoorLetter}e` + `, please`
